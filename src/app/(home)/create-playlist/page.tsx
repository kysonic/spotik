import CreatePlaylist from '@/components/features/playlist/CreatePlaylist';

export default async function CreatePlaylistPage() {
  return (
    <main className="h-full">
      <CreatePlaylist />
    </main>
  );
}
